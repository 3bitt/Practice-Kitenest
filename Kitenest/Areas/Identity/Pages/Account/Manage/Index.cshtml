@page
@model IndexModel
@{
    ViewData["Title"] = "Profile";

    var user = @ViewData["user"];
}

<h4>@ViewData["Title"]</h4>

@{ if (ViewData["statusMessage"] != null) { 
    <div class="text-success">
        @ViewData["statusMessage"]
    </div>
    }
}
<div class="row">
    <div class="col-md-3"
        <label asp-for="Input.Email"></label>
        <input asp-for="User.Identity.Name" class="form-control" ReadOnly="true" />
    </div>
</div>

<div class="row" style="padding-top:10px">
    <a asp-page="./ChangePassword">Change password</a>

</div>


@*
    <div class="row">
        <div class="col-md-6">
            <form id="profile-form" method="post">
                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Username"></label>
                    <input asp-for="Username" class="form-control" disabled />
                </div>
                <div class="form-group">
                    <label asp-for="Input.Email"></label>
                    <input asp-for="Username" class="form-control" disabled />
                </div>
                <div class="form-group">
                    <label asp-for="Input.PhoneNumber"></label>
                    <input asp-for="Input.PhoneNumber" class="form-control" />
                    <span asp-validation-for="Input.PhoneNumber" class="text-danger">Optional</span>
                </div>
                <button type="submit" class="btn btn-default">Save</button>
            </form>
        </div>
    </div>

    @section Scripts {
        <partial name="_ValidationScriptsPartial" />
    }
*@